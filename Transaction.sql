SET XACT_ABORT ON

BEGIN TRANSACTION
	UPDATE	Players
	SET		Balance = Balance - 1
	WHERE	ID = 1
	
	UPDATE	Players
	SET		Balance = Balance + 1 / 0
	WHERE	ID = 2
COMMIT TRANSACTION

GO

SELECT * FROM Players